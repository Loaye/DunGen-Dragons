//need all races and classes


  var allRaces = ['dragonborn', 'dwarf', 'elf', 'half-orc', 'halfling', 'human'];
  var allClasses = ['bard', 'cleric', 'mage', 'ranger', 'rogue', 'warrior'];

  //need to combine race + class
  //builderResults = race + class

  var builderResults = function(races, classes) {
    alert('You are ' + player.name + ', the ' + player.race + ' ' + player.class + '.')
    }

  builderResults()
